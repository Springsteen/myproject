function logger(message) {
  console.log(message);
}

function testFunction (pattern) {
  let myarr = [];
  myArr.push(1);
  myArr.push(2);
  myArr.push(3);

  let filteredArr = myArr.filter((elem) => {
    return elem === pattern;
  });
  
  retrun myarr;
}
